spring:
  datasource:
         url: jdbc:mysql://localhost:3306/shiro
         username: root
         password: root
  thymeleaf:
         cache: true
         check-template-location: true
         content-type: text/html
         enabled: true
         encoding: utf-8
         mode: HTML5
         prefix: classpath:/templates/
         suffix: .html
# 为什么下面的cache配置路径加上classpath*:就总是无法启动呢？
  cache:
        ehcache:
          config:  ehcache.xml
debug:
   true
mybatis:
  mapper-locations: classpath*:mapping/*Mapper.xml
